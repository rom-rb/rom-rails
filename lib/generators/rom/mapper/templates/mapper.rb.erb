ROM.mappers do

  define(:<%= class_name.underscore %>) do
    # specify model and attributes ie
    #
    # model <%= class_name.classify %>
    #
    # attribute :name
    # attribute :email
  end

end
