class Create<%= model_name %> < ROM::Commands::Create<%= "[:#{adapter}]" %>
  relation :<%= relation %>
  register_as :create
  result :one

  # Override to specialize this command
  def execute(tuples)
    super tuples
  end

end
