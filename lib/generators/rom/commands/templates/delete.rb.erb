class Delete<%= model_name %> < ROM::Commands::Delete<%= "[:#{adapter}]" %>
  relation :<%= relation %>
  register_as :delete
  result :one

  # Override to specialize this command
  def execute(tuples)
    super tuples
  end

end
