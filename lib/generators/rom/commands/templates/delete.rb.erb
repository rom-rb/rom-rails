class <%= class_name.singularize.camelcase %>Commands::Delete < ROM::Commands::Delete<%= "[:#{options[:adapter]}]" if options[:adapter] %>
  relation :<%= class_name.pluralize.underscore %>
  register_as :delete
  result :one
end
