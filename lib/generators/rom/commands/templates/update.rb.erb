class Update<%= model_name %> < ROM::Commands::Update<%= "[:#{adapter}]" %>
  relation :<%= relation %>
  register_as :update
  result :one

  # Override to specialize this command
  def execute(tuples)
    super tuples
  end

end
