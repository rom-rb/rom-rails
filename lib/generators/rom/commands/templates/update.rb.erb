class Update<%= model_name %> < ROM::Commands::Update<%= "[:#{adapter}]" %>
  relation :<%= relation %>
  register_as :update
  result :one

  # set Timestamp plugin
  # use :timestamps
  # timestamp :updated_at
end
