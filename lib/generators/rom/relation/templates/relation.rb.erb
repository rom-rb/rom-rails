class <%= class_name.camelcase %>Relation < ROM::Relation<%= "[:#{options[:adapter]}]" %>
  dataset :<%= class_name.underscore.pluralize %>

  # define your methods here ie:
  #
  # def all
  #   select(:id, :name).order(:id)
  # end
end
