class <%= class_name.camelcase %> < ROM::Relation<%= "[:#{options[:adapter]}]" if options[:adapter] %>
  # define your methods here ie:
  #
  # def all
  #   select(:id, :name).order(:id)
  # end
end
